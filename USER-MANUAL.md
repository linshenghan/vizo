# Opus V6 用户手册

## 功能清单

### 核心功能

#### 工作流进度可视化
- **功能说明**：实时显示 Agent 执行进度的终端表格
- **支持的列**：
  - `#`：序号
  - `角色`：Agent 角色名称
  - `模型`：使用的 LLM 模型（如 opus→haiku 表示降级）
  - `状态`：⏳ 运行中 / ✅ 完成 / ❌ 失败
  - `启动`：Agent 启动时间（HH:MM:SS）
  - `耗时`：运行耗时（实时更新，格式 Xm YYs）
  - `费用`：美元消耗（精确到小数点后两位）
  - `产出文件`：输出文件名（可点击预览链接）
  - `当前动作`：运行中 Agent 的当前操作描述
- **自动刷新**：每秒刷新一次，实时计时

#### 多模型降级链
- 首选模型不可用时自动降级：`Opus → Sonnet → Haiku`
- 模型列显示格式：`opus→haiku` 表示已从 Opus 降级到 Haiku
- 支持按角色自定义模型策略（通过 config.json）

#### 并行任务执行
- 最多支持 3 个并行 Agent（可配置）
- 每个并行 Agent 使用独立的 Git Worktree
- 自动合并 Worktree 回主分支

#### 中断恢复
- 支持任意步骤中断后恢复执行
- **恢复逻辑**：从最后一个完成的步骤之后继续执行，不重新运行已完成的步骤
- 支持子任务中断恢复：子任务中断后 resume 时，也从上次完成步骤继续
- 命令：`opus --resume`

#### Web 任务控制台
- **访问地址**：`https://opus.bingbing.asia/v6/tasks`（及 `/v6/tasks/{task_id}` 详情页）
- **功能**：浏览器操控任务的暂停、恢复、回退、终止
- **确认链接过期**：自动跳转到任务控制台，不再显示空白过期页面

#### Web Console 认证与安全
- **认证机制**：采用 bcrypt 密码哈希验证，不存储明文密码
- **首次访问**：全新部署时必然显示 Setup Wizard 引导页，初始化系统配置
- **密码修改**：在设置页支持密码修改功能
- **会话管理**：修改密码后，旧 Cookie 在 5 秒宽容期后失效，防止旧会话被滥用
- **受保护路由**：未完成 Setup Wizard 引导时，所有受保护路由自动重定向到引导页

#### 设置页新增功能
- **密码修改**：支持修改 Web Console 的访问密码
- **模型配置**：支持在 Web 页面修改 LLM 模型偏好（如优先使用 Sonnet 而非 Opus）
- **实时生效**：模型配置修改后实时写入 config.json 并热加载，无需重启服务
- **网络 Tab**：新增公网域名配置，用于设置文档预览和确认页面链接的域名

#### 工作流快捷按钮区
- **位置**：Web Console 底部，输入框下方
- **快捷按钮**：6 个工作流类型快捷按钮
  - `新功能`（feature）
  - `缺陷修复`（bug_fix）
  - `代码重构`（refactor）
  - `紧急修复`（hotfix）
  - `性能优化`（optimization）
  - `自动分类`（auto）
- **操作方式**：点击按钮后，自动填入工作流前缀到输入框（如 `--workflow feature `），用户只需输入任务描述即可
- **功能优势**：快速启动指定工作流任务，无需手动输入 `--workflow` 参数

#### 预览文件持久化
- 预览文件写入磁盘，重启服务后仍可通过原链接访问（此前仅存 Redis，重启后丢失）

#### 移动端控制台
- **功能说明**：针对移动设备优化的任务控制台，支持在手机/平板上浏览和操控任务
- **访问地址**：`https://opus.bingbing.asia/v6/m`（移动端主页）及 `/v6/m/login`（移动端登录页）
- **支持功能**：
  - 浏览任务列表和详情
  - 暂停/恢复任务
  - 查看实时工作流进度
  - 费用统计查看
- **首次访问**：未初始化时自动重定向到 `/v6/console/setup` 进行初始化设置
- **响应式设计**：自适应各尺寸移动设备，提供最优用户体验
- **路由扩展**：confirm_server 启动时额外注册 3 条 Mobile 路由，现有路由行为保持不变

#### 直播页面（实时工作流可视化）
- **功能说明**：全屏实时展示任务执行进度和总结
- **启动命令**：`opus --stream "任务描述"`
- **显示内容**：
  - **任务类型标签**：顶部显示任务类型（如"缺陷修复（bug_fix）"）
  - **工作流进度表**（实时更新，含角色、模型、状态、耗时、费用、产出文件）
  - **完整工作流**：根据 task_type 渲染待执行步骤，预览后续流程
  - **任务总结表格**（任务结束时显示）
  - **完整成本统计**（每个 Agent 的 Token、耗时、费用）
- **回放功能**：`opus --stream --replay <task_id>`
- **表格内容**：角色、使用模型、执行状态、耗时、消耗费用、产出文档、是否有预览链接
- **自动生成**：任务执行完毕后自动生成总结表格，无需手动操作

### Hub 任务（专业模块工作流）

Hub 任务是针对特定领域（合同服务、财务分析等）的专业工作流，相对于 Dev 工作流的轻量级任务类型。

#### Hub 任务特性

**manifest 驱动的工作流**：
- 每个 hub 模块通过 manifest 文件定义完整的工作流步骤
- manifest 中 `confirm: true` 的步骤完成后自动暂停等待用户确认
- 支持 Web 页面确认

**步骤粒度的任务控制**：
- **暂停/终止**：hub 任务执行中可随时暂停或终止，当前步骤完成后立即停止
- **步骤级回退**：支持回退到指定步骤重新执行，例如 `opus --rollback hub-xxx --step draft`
- **确认反馈注入**：用户在确认时提供的修改意见，自动注入后续步骤的 AI Prompt，确保修改意见被体现

**知识管理隔离**：
- dev 项目中的 hub 任务知识隔离存储到 `.serena/memories/hub/{module_id}/` 目录
- 每个 hub 模块的知识独立管理，不污染项目通用知识库

**自动知识提取**：
- 满足触发条件时自动执行 knowledge_extractor，提取并沉淀领域知识
- 后续任务自动加载提取的知识上下文

**详细费用明细**：
- 任务完成摘要新增每步骤的费用明细展示
- 帮助用户了解各步骤的成本分布

#### Hub 任务执行流程

```
1. 用户发送 hub 任务请求
  ↓
2. 系统加载对应 module 的 manifest 文件
  ↓
3. 按 manifest 定义的步骤依次执行
  ↓
4. 遇到 confirm: true 的步骤完成后暂停等待确认
  ↓
5. 用户通过 Web 页面确认（可提供修改意见）
  ↓
6. 修改意见注入后续步骤的 Prompt
  ↓
7. 继续执行后续步骤
  ↓
8. 自动执行 knowledge_extractor 提取知识
  ↓
9. 生成任务摘要（包含每步骤费用明细）
  ↓
10. 完成
```

#### Hub 任务中断恢复

```bash
# 暂停运行中的 hub 任务
opus --pause <task_id>

# 从暂停状态恢复
opus --resume --task-id <task_id>

# 回退到指定步骤重新执行
opus --rollback <task_id> --step <step_name>

# 示例
opus --rollback hub-20260317-xyz --step draft
```

### 角色支持

系统内置 18 个专业角色，各有其专属的行为准则和工具权限：

| 角色 | 用途 | 权限 |
|------|------|------|
| 需求分析师 | 理解需求、识别问题 | 读、搜索代码 |
| 产品经理 | 编写 PRD 需求文档 | 读、搜索代码 |
| 项目经理 | 拆分大需求为多个子任务 | 读、搜索代码 |
| 架构师 | 设计技术方案 | 读、搜索代码、Serena |
| 后端开发 | 开发后端代码 | 读、写、编辑、执行、Bash |
| 前端开发 | 开发前端代码 | 读、写、编辑、执行、Bash |
| 嵌入式开发 | 处理嵌入式/IoT 问题 | 读、写、编辑、执行、Bash |
| 联调工程师 | 处理前后端接口冲突 | 读、写、编辑、执行、Bash |
| QA 测试 | 编写测试用例、执行测试 | 读、执行 |
| 修复工程师 | 修复测试失败的代码 | 读、写、编辑、执行 |
| 运维工程师 | 部署到生产环境 | 读、执行 |
| 知识工程师 | 沉淀任务知识到项目记忆、维护 tech-stack.md | 读、Serena |
| 代码探索 | 分析代码结构（只读） | 读、搜索代码 |
| 助手 | 快速问答 | 读、Serena |

## 操作指南

### 启动任务

```bash
# 基本形式：启动一个新任务
opus "任务描述"

# 示例
opus "给小智添加定时关机功能"

# 后台运行（推荐）
python3 /opt/opus-v6/opus.py "任务描述" &

# 指定工作流类型（跳过 AI 自动分类）
opus --workflow feature "添加用户认证功能"
opus --workflow bug_fix "修复登录页面崩溃"
opus --workflow refactor "重构用户管理模块"
opus --workflow hotfix "紧急修复支付流程"
opus --workflow optimization "优化数据库查询性能"
```

**--workflow 参数说明**：
- 启用时：跳过 AI 自动分类阶段，直接路由到指定工作流（architect 架构设计阶段仍会执行）
- 支持的类型：`feature`（新功能） | `bug_fix`（缺陷修复） | `refactor`（代码重构） | `hotfix`（紧急修复） | `optimization`（性能优化）
- 如不指定，系统自动分类（值为 `auto`）
- **优势**：跳过分类阶段，节省成本和时间（约节省 $0.01~0.02）

### 直播模式

```bash
# 全屏直播任务执行过程（实时工作流表 + 任务总结表）
opus --stream "任务描述"

# 回放历史任务的执行过程
opus --stream --replay <task_id>

# 示例
opus --stream "添加用户认证功能"
opus --stream --replay 20260219-195040-j3h7
```

**直播页面说明**：
- 实时显示工作流进度表，每秒自动刷新
- 当任务完成时，自动显示**总结表格**，包含：
  - 每个 Agent 的角色名、使用模型、执行状态、耗时、费用
  - 输出文档名称和预览链接
  - 任务总费用和总耗时统计
- 回放功能允许查看历史任务的完整执行过程和最终总结

### 中断恢复

```bash
# 恢复最后一个未完成的任务
opus --resume

# 也可后台运行
python3 /opt/opus-v6/opus.py --resume &
```

### Hub 任务操作

```bash
# 启动 hub 任务（指定模块）
opus "任务描述" @contract_service    # 合同服务模块
opus "任务描述" @other_module        # 其他 hub 模块

# 暂停运行中的 hub 任务
opus --pause <task_id>

# 恢复暂停的 hub 任务
opus --resume --task-id <task_id>

# 回退到指定步骤重新执行
opus --rollback <task_id> --step <step_name>
```

**Hub 任务说明**：
- Hub 任务支持在确认后提供修改意见，修改意见会自动注入后续步骤
- 各 hub 模块的知识独立存储在 `.serena/memories/hub/{module_id}/` 目录
- 任务完成后生成的摘要包含每个步骤的详细费用统计
- 步骤支持粒度化的中断和回退操作

**确认流程**：
- manifest 中 `confirm: true` 的步骤完成后自动暂停
- 用户可通过 Web 确认页面进行确认
- 支持提供修改意见，后续步骤自动体现用户反馈

### 快速问答（不走工作流）

```bash
# 直接问答，无需经过完整的工作流
opus --chat "什么是MVC 架构？"
```

### 查看 Agent 模块列表

```bash
# 列出系统可用的 Agent 模块
opus agents

# 输出 JSON 格式（用于脚本集成）
opus agents --json
```

**模块列表说明**：
- 各模块的功能描述和工作流说明
- JSON 格式输出便于第三方系统集成
- 模块信息每次执行 opus 命令时自动同步到 CLAUDE.md

### 代码探索（只读分析）

```bash
# 分析代码结构，不修改代码
opus "怎么实现的用户登录流程？"
```

### 查看任务历史

```bash
# 查看所有任务记录
opus --history

# 查看今日消耗（精确到模型分类）
opus --cost
```

**历史表格说明**：

任务历史表显示 5 列信息：

| 列名 | 说明 | 示例 |
|------|------|------|
| `ID` | 任务唯一标识符 | `20260219-174647-kngi` |
| `描述` | 任务简述（最多 36 字符） | `opus --history 表格优化` |
| `耗时` | 执行耗时 | `3m 45s` / `⏳ 运行中` |
| `状态` | 任务完成状态 | `✅ 完成` / `❌ 失败` |
| `产出` | 输出文件数量 | `5 files` |

**耗时显示规则**：
- 已完成任务：显示总耗时（格式 `Xm YYs`）
- 运行中任务：显示 `⏳ 运行中`，实时更新

**任务列表排序**：
- **智能排序**：进行中任务（running/paused）优先显示
- **次级排序**：按创建时间倒序，最新的在前
- **优势**：活跃任务始终位于列表顶部，便于快速定位和操作

**任务名称展示**：
- 优先使用 AI 提炼的 task_name（更精准简洁）
- 降级使用 description（原始任务描述）

### 移动端控制台访问

```bash
# 移动设备浏览器访问（自动重定向到登录页）
https://opus.bingbing.asia/v6/m

# 直接访问登录页
https://opus.bingbing.asia/v6/m/login
```

**首次访问流程**：
1. 移动设备访问 `/v6/m` 主页
2. 系统检测初始化状态
3. 未初始化时自动重定向至 `/v6/console/setup` 完成初始化
4. 初始化完成后可正常使用移动端功能

**功能说明**：
- 与桌面版 Web Console 共享认证体系（同一套密码）
- 支持暂停/恢复/终止任务操作
- 实时查看工作流进度和费用统计
- 自适应移动设备屏幕尺寸

### 部署方式

#### Docker Compose 一键部署

```bash
# 完整堆栈部署（包含应用、Redis、Web Console）
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

此方式最快速，适合快速评测和生产部署。

#### 本地开发运行

```bash
# 启动应用和服务
python3 /opt/opus-v6/opus.py

# 后台运行
python3 /opt/opus-v6/opus.py &
```

### 工作流进度表操作

进度表在任务运行时自动显示，无需手动操作：

1. **刷新速度**：每秒自动刷新一次
2. **模型列展示**：
   - `opus` 表示使用 Opus 模型
   - `opus→sonnet` 表示从 Opus 降级到 Sonnet
   - `opus→haiku` 表示两次降级到 Haiku
3. **耗时计算**：运行中实时计算，以分:秒格式显示
4. **费用统计**：汇总行显示全部 Agent 总成本
5. **产出文件**：可点击预览（如果提供了预览链接）

#### Web 确认页面信息卡片
确认页面新增 `context-card` 信息卡片区域，显示：
- **角色**：执行该步骤的 Agent 角色
- **费用**：到该步骤的累计消耗
- **进度**：任务整体进度百分比
- 帮助用户更好理解当前的上下文和成本

### 终端模式确认提示增强

终端模式确认提示现已集成上下文信息：

```
==================================================
需求分析报告已生成

任务: 给小智添加定时关机功能
角色: 需求分析师
步骤: 1/8
费用: $0.12 | Token: 2.5K→8.4K | 耗时: 2m 15s
累计: $0.12 (总耗时: 2m 15s)
进度: 12%

📎 在线预览：https://opus.bingbing.asia/preview/a1b2c3d4
📁 本地文件：/opt/opus-v6/.opus/tasks/20260219-044607/00-requirement-analysis.md
==================================================
[y] 确认  [n] 取消  [f] 修改意见: _
```

信息项说明：
- **任务**：当前执行的任务名称
- **角色**：执行该步骤的 Agent 角色
- **步骤**：当前步骤/总步数
- **费用**：当前 Agent 消耗及 Token 数
- **累计**：任务总消耗及总耗时
- **进度**：百分比进度条

### 跨进程通知信息增强

stop_guard hook 的通知消息现包含完整的任务上下文：
- **任务名称**：任务简述
- **角色**：执行中的 Agent 角色
- **步骤**：工作流步骤号
- **费用**：当前消耗统计

stderr 标记格式已扩展为 6 段：
```
OPUS_CONFIRM_PENDING|{request_id}|{type}|{message_summary}|{role_display}|{step_display}
```

新增的最后两段：
- `role_display`：角色显示名称
- `step_display`：步骤显示（如 "2/5"）

### 费用详情文件（cost.json）

每个任务执行完毕后生成 `cost.json` 文件，记录详细的成本信息：

```json
[
  {
    "role": "architect",
    "model": "opus",
    "input_tokens": 14,
    "output_tokens": 10757,
    "cost_usd": 1.342084,
    "timestamp": "2026-02-19T20:18:46",
    "duration": "5m 30s",           // [可选] 执行耗时
    "output_file": "02-design.md",  // [可选] 输出文件名
    "preview_url": "https://opus.bingbing.asia/preview/a1b2c3d4"  // [可选] 在线预览链接
  }
]
```

**字段说明**：
- `role`：执行该步骤的 Agent 角色
- `model`：使用的 LLM 模型
- `input_tokens`：输入 token 数
- `output_tokens`：输出 token 数
- `cost_usd`：美元消耗
- `timestamp`：执行时刻
- `duration`（可选）：Agent 执行耗时
- `output_file`（可选）：生成的输出文件名
- `preview_url`（可选）：文件在线预览链接

**使用场景**：
- 直播页面总结表格从 cost.json 读取数据生成
- 任务历史统计从 cost.json 聚合计算
- 与外部系统集成时获取完整的执行信息

### 任务确认

任务执行过程中多次需要用户确认，支持终端与 Web 页面两种模式。**系统特性**：确认请求创建后会在秒级内通过 stderr 通知转发到主代理，确保用户及时感知待确认任务。



#### 终端模式

```
需求分析报告已生成

   📎 在线预览：https://opus.bingbing.asia/preview/a1b2c3d4
   📁 本地文件：/opt/opus-v6/.opus/tasks/20260219-044607/00-requirement-analysis.md
   [y] 确认  [n] 取消  [f] 修改意见: _
```

输入 `y` 继续，`n` 取消，`f` 提供修改意见（最多 2 轮）。

#### Web 确认页面

点击发送的链接进入 Web 确认页面，支持：
- 确认继续
- 取消任务
- 提交修改意见

#### 随时中断

在任何确认等待或任务运行中，可通过 Web 控制台暂停任务，或在终端使用：
- `opus --pause <task_id>`

#### 终止任务（需二次确认）

在任务运行或暂停状态中，可通过 Web 控制台终止任务，或在终端使用：
- `opus --terminate <task_id>`

## 工作流说明

### 完整开发流程（新功能）

```
需求分析（确认）
  ↓
产品经理写 PRD（确认）
  ↓
架构师设计方案（确认）
  ↓
并行开发（后端 + 前端 + 测试用例）
  ↓
联调工程师处理接口
  ↓
测试-修复循环（最多 3 轮）
  ↓
部署（确认）
  ↓
知识沉淀
  ↓
完成
```

#### 测试-修复循环说明

**QA 测试范围判定**：
- 子任务的 QA 测试采用范围内判定逻辑
- 通过条件：测试通过率 >= 90% **且** 修改范围内无失败
- 范围外失败（回归问题）记为警告，不阻止任务继续
- 此逻辑为默认行为，无需用户干预

**Bug-fix 循环派发**：
- 修复工程师只收到修改范围内的测试失败
- 范围外的失败作为回归警告单独汇报
- 优化修复范围，提升修复效率

### 快速修复流程

```
需求分析（确认）
  ↓
架构师分析（确认）
  ↓
修复工程师修改代码
  ↓
测试-修复循环（最多 3 轮）
  ↓
部署（确认）
  ↓
知识沉淀
  ↓
完成
```

### 大需求多任务流程

```
需求分析（确认）
  ↓
产品经理拆分任务（确认）
  ↓
架构师全面设计（确认）
  ↓
按依赖顺序执行各子任务
  （每个子任务走自己的工作流）
  ↓
全量集成测试
  ↓
部署（确认）
  ↓
知识沉淀
  ↓
完成
```

## 费用统计

系统精确计算每个 Agent 的消耗，按模型区分价格：

| 模型 | 输入价格 | 输出价格 |
|------|----------|----------|
| Opus | $15/M tokens | $75/M tokens |
| Sonnet | $3/M tokens | $15/M tokens |
| Haiku | $0.25/M tokens | $1.25/M tokens |

### 查看费用

```bash
# 查看今日消耗统计
opus --cost

# 任务目录下的成本记录
cat .opus/tasks/{task_id}/cost.json
```

### 预算告警

- 每日预算限制：默认 $50（可在 config.json 修改）
- 警告阈值：达到 80% 时提示
- 停止阈值：达到 95% 时停止新任务

## 知识沉淀

完成任务后，系统自动沉淀关键知识到项目记忆库：

| 记忆类型 | 内容 | 来源 |
|----------|------|------|
| project-overview | 项目基本信息 | 首次创建 |
| architecture | 系统架构、模块结构 | 架构师设计 |
| code-style | 代码规范、命名约定 | 开发过程 |
| common-pitfalls | 常见问题、踩坑记录 | 每个任务沉淀 |
| design-principles | 设计原则、技术决策 | 架构师总结 |
| tech-stack | 项目技术栈与依赖列表 | 系统自动扫描 |

后续新任务自动加载这些记忆，提升 Agent 的理解度。

### 技术栈自动检测与注入

**首次运行检测**：
- 当首次在新项目中运行 opus 任务时，系统自动扫描项目文件（package.json、go.mod、requirements.txt、build.gradle 等）
- 生成 `tech-stack.md` 记忆文件，记录项目技术栈和依赖关系
- 自动存入项目 Serena 记忆库

**技术栈上下文注入**：
- 7 个技术相关角色（架构师、后端开发、前端开发、嵌入式开发、联调工程师、QA 测试、修复工程师）的 Prompt 中自动包含项目技术栈上下文
- 提升 Agent 的技术匹配度和代码质量
- 减少不必要的问题诊断步骤

**维护权限**：
- **知识工程师**可以读取和更新 tech-stack.md，保持技术栈信息的时效性
- 当项目依赖升级、新增技术栈后，知识工程师可维护对应记忆，后续任务自动使用最新的技术栈信息

## 配置文件

### 敏感字段管理

为保护隐私，config.json 中所有敏感字段都使用占位符值（如 `YOUR_REDIS_PASSWORD`），真实值通过环境变量传入：

```json
// config.json - 示例（占位符形式）
{
  "redis": {
    "host": "localhost",
    "port": 6379,
    "password": "YOUR_REDIS_PASSWORD"
  }
}
```

### 环境变量覆盖机制

系统支持多个环境变量覆盖 config.json 的对应字段：

| 环境变量 | 覆盖字段 | 说明 |
|---------|---------|------|
| `REDIS_HOST` | `redis.host` | Redis 服务器地址 |
| `REDIS_PORT` | `redis.port` | Redis 端口号 |
| `REDIS_PASSWORD` | `redis.password` | Redis 密码 |
| `CLAUDE_API_KEY` | `api.claude_key` | Claude API 密钥 |
| `WEB_CONSOLE_PASSWORD` | `web_console.password` | Web Console 访问密码 |
| `DEFAULT_MODEL` | `default_model` | 默认使用的 LLM 模型 |

**使用示例**：

```bash
# 通过环境变量配置 Redis
export REDIS_HOST=127.0.0.1
export REDIS_PORT=6379
python3 /opt/opus-v6/opus.py "任务描述"

# 或在启动命令中直接指定
REDIS_HOST=127.0.0.1 REDIS_PORT=6379 python3 /opt/opus-v6/opus.py "任务描述"
```

### config.json 完整结构

```json
{
  "default_project": "xiaozhi-server",
  "model_overrides": {
    "backend_developer": "sonnet"
  },
  "max_concurrent_agents": 3,
  "budget": {
    "daily_limit_usd": 50,
    "warn_threshold": 0.8,
    "stop_threshold": 0.95
  },
  "ui_mode": "terminal",
  "redis": {
    "host": "localhost",
    "port": 6379,
    "password": "YOUR_REDIS_PASSWORD"
  },
  "web_console": {
    "port": 9380,
    "password": "YOUR_CONSOLE_PASSWORD"
  }
}
```

## 故障排除

### 任务卡住无反应

1. 检查进度表最后一个 Agent 的耗时是否持续增长
2. 如果超过预期时间（通常 10 分钟），通过 Web 控制台暂停，或使用 `opus --pause <task_id>` 中断
3. 执行 `opus --resume` 恢复，系统将从断点继续

### 模型限流或超时

系统自动降级到备用模型，进度表的模型列会显示 `opus→sonnet→haiku`。如果全部模型都不可用，系统会暂停并保存状态，待恢复后继续。

### 测试循环失败 3 次

任务会自动回滚，所有代码变更恢复到任务开始前的状态。查看 `.opus/tasks/{task_id}/99-failure-report.md` 了解失败原因。

### 语义路由 API 失败

当 `opus "任务"` 执行时，系统会自动调用语义路由 API 判断任务属于哪个 Agent 模块（如合同服务、开发任务等）。

**失败行为**：

如果路由 API 不可用（网络错误、超时、认证失败等）：
1. 系统会输出错误提示信息
2. 列出所有可用的 Agent 模块列表
3. 停止执行，等待用户使用 `@模块ID` 重新指定模块

**解决步骤**：

```bash
# 1. 查看可用模块列表
opus agents

# 2. 根据任务性质选择对应模块，重新执行
opus "任务描述" @contract_service   # 合同相关任务
opus "任务描述"                      # 不指定模块则走开发流程
```

**认证配置**：

- 路由 API 认证信息优先从 `settings.json`（Claude Code 用户配置）读取
- 次级读取 config.json
- 确保 `settings.json` 中包含有效的认证信息，可咨询系统管理员

**超时设置**：

- 路由 API 请求超时时间为 20 秒
- 如频繁超时，检查网络连接和 API 服务器状态

## 变更日志

### 2026-03-20（第十二次更新 - Agent 模块管理、项目类型与模型可视化配置）

#### 新增：Web Console Agent 模块管理
- **变更说明**：Web Console 侧边栏新增"Agent 模块"入口，用户可直接在浏览器中管理和配置 Agent 模块
- **功能清单**：
  - 模块列表：展示所有可用的 Agent 模块
  - 模块详情抽屉：显示工作流步骤、功能说明、输入输出等完整信息
  - 搜索/筛选：快速查找特定模块
- **应用场景**：用户无需通过 CLI 命令，直接在 Web 界面浏览和管理 Agent 生态

#### 新增：AI 辅助 Agent 创建（Web + 移动端）
- **变更说明**：支持 Web 与移动端的 AI 辅助创建 Agent 模块，降低定制化开发的技术门槛
- **Web 端流程**：输入描述 → AI 生成模块代码 → 预览编辑 → 保存使用
- **移动端流程**：在"我的"页面 → Agent 管理 → 创建 Agent → 3 步完成（描述、工作流、保存）
- **核心优势**：无需手写代码，用户通过自然语言描述即可生成定制化 Agent
- **使用场景**：快速响应新业务需求，创建领域特定的工作流

#### 新增：项目类型选择与标签显示
- **变更说明**：项目创建时支持选择项目类型，项目列表中显示对应标签
- **项目类型**：
  - **开发项目**：标准开发工作流，自动执行 `git init` 初始化版本控制
  - **工作区**：Hub 模块工作区，不执行 git init，专用于 Hub 任务（如合同审查）
- **项目列表标签**：每个项目卡片显示项目类型标签，一目了然
- **建议用途**：
  - 开发项目：传统软件开发任务
  - 工作区：专业领域工作流、无代码输出的任务

#### 新增：Hub 任务在任务列表中的混合展示
- **变更说明**：任务列表支持混合展示 dev 任务和 hub 任务，并通过视觉标识区分
- **Hub 任务标识**：
  - 绿色边框：直观指示该任务为 Hub 专业任务
  - 🤖 机器人图标：表示使用 AI 模块工作流
  - 模块名标签：显示所属的 Hub 模块名称（如"合同服务""财务分析"）
- **任务详情扩展**：Hub 任务详情页支持查看各步骤产出的 Markdown 预览
  - 用户可直接在 Web 界面浏览步骤成果，无需下载文件
  - 便于快速审阅和反馈

#### 新增：模型配置可视化页面
- **变更说明**：Web Console 新增模型配置可视化页（或扩展现有设置页），支持 GUI 配置 model_overrides
- **功能说明**：
  - 展示所有可配置的角色列表
  - 为每个角色选择优先使用的 LLM 模型（Opus、Sonnet、Haiku）
  - 支持全局默认模型设置
  - 实时生效，无需重启应用
- **用户体验**：从手动编辑 JSON 改为可视化界面操作，大幅降低配置难度
- **高级用途**：用户可根据成本预算灵活调整模型策略

#### 新增：移动端"我的"页面扩展
- **变更说明**：移动端底部新增"我的"Tab，包含三个核心入口
- **页面内容**：
  - **对话**：快速问答、任务记录回顾
  - **任务**：进行中的任务、历史任务浏览
  - **我的**：Agent 管理、设置、账户信息等
- **Agent 管理入口**：在"我的"页面内集成 AI 辅助创建 Agent 的快捷方式
- **触屏优化**：为移动设备优化的 Tab 导航，便于在手机上快速切换功能

#### 改进：项目创建路径动态化
- **变更说明**：项目创建不再硬编码使用 `/opt/opus-v6/projects/` 路径，改为使用动态路径配置
- **优势**：
  - 支持多个安装位置，提升部署灵活性
  - 用户可通过配置指定项目存储位置
  - 便于容器化部署和多用户场景

#### 影响范围
- `lib/web_console.py`：Agent 模块管理 UI、AI 辅助创建 UI、模型配置可视化页
- `lib/mobile_console.py`：移动端"我的"Tab、Agent 管理入口、3 步创建流程
- `orchestrator.py`：项目类型路由、Hub 任务与 Dev 任务的混合调度
- `state_manager.py`：项目类型存储、任务类型标记（dev/hub）
- 配置文件：动态项目路径配置

#### 向后兼容性
- 现有开发项目自动标记为"开发项目"类型，行为保持不变
- 现有任务列表展示逻辑保持兼容，仅新增 Hub 任务标识
- 模型配置可视化为可选功能，不影响现有配置文件的使用

#### 用户影响总结
- **更友好的界面**：Agent 管理、模型配置从 CLI 转移到可视化 Web 界面
- **更低的门槛**：AI 辅助创建 Agent，用户无需理解代码细节
- **更灵活的工作流**：项目类型选择、Hub 任务混合展示，支持多样化业务需求
- **更统一的体验**：Web 与移动端同步功能支持

---

### 2026-03-17（第十一次更新 - Hub 任务工作流升级与知识隔离）

#### 新增：Hub 任务暂停/终止行为
- **变更说明**：hub 任务新增暂停和终止功能，执行中可随时发出信号，当前步骤完成后立即停止
- **命令格式**：
  - `opus --pause <task_id>`：暂停运行中的 hub 任务
  - `opus --resume --task-id <task_id>`：恢复暂停的 hub 任务
- **应用场景**：hub 任务执行时如需修改参数或中断流程，可直接暂停而无需等待当前步骤完成

#### 新增：Manifest 步骤级确认机制
- **变更说明**：hub 任务的 manifest 文件支持 `confirm: true` 字段，标记需要用户确认的步骤
- **确认流程**：step 完成后自动暂停，等待用户通过 Web 页面进行确认
- **Web 支持**：用户可在 Web 确认页面完成确认、取消或填写修改意见
- **实现原理**：修改了任务完成后的暂停逻辑，支持步骤粒度的确认截断

#### 新增：确认时修改意见反馈注入
- **变更说明**：用户在确认步骤时可选提供修改意见，后续步骤的 AI 输出会自动体现这些意见
- **操作方式**：在 Web 确认页面选择"修改意见"选项，输入反馈内容
- **反馈注入**：修改意见自动注入后续步骤的 Prompt 上下文，确保 AI 产出符合用户期望
- **应用场景**：合同审查、需求分析等需要多轮迭代确认的专业模块
- **影响范围**：orchestrator.py 的确认反馈处理、agent_runner.py 的 Prompt 注入

#### 新增：Hub 任务步骤级回退
- **变更说明**：hub 任务支持回退到指定步骤重新执行，而不必重新开始整个任务
- **命令格式**：`opus --rollback <task_id> --step <step_name>`
- **示例**：`opus --rollback hub-20260317-xyz --step draft` — 回退到 draft 步骤并重新执行
- **使用场景**：某个步骤输出不满意时，可直接回退到该步骤而非从头开始
- **对比 dev 任务**：dev 任务支持粗粒度回退（整个阶段），hub 任务支持更细粒度的步骤回退
- **影响范围**：state_manager.py 的回退逻辑、orchestrator.py 的步骤恢复

#### 新增：Hub 任务知识隔离
- **变更说明**：dev 项目中的 hub 任务知识隔离存储，不与项目通用知识库混合
- **存储路径**：`.serena/memories/hub/{module_id}/` 目录（例如 `.serena/memories/hub/contract_service/`）
- **知识范围**：每个 hub 模块的领域知识、历史案例、专业规范等
- **优势**：防止不同领域知识污染，提升知识查询的精准度
- **后续效果**：新任务加载对应 hub 模块的知识时，优先从隔离目录读取

#### 新增：自动知识提取（Knowledge Extractor）
- **变更说明**：hub 任务执行完成后，满足触发条件时自动执行 knowledge_extractor
- **提取内容**：领域知识、专业规范、常见问题、最佳实践等
- **存储位置**：自动沉淀到 `.serena/memories/hub/{module_id}/` 隔离目录
- **触发条件**：任务类型、步骤数、输出复杂度等多因素综合判定
- **后续价值**：提取的知识自动用于后续同类型任务，提升 AI 的专业度

#### 改进：任务完成摘要费用明细
- **变更说明**：hub 任务完成后生成的摘要新增每个步骤的费用明细展示
- **显示内容**：每个步骤的 Agent 角色、使用模型、Input/Output tokens、成本、执行时间
- **应用场景**：帮助用户理解任务成本分布，优化步骤配置以降低成本
- **实现方式**：从 cost.json 聚合计算，按步骤顺序排列展示
- **对比原有设计**：原有设计仅显示整个任务的总成本，新设计提供粒度化的成本分析

#### 影响范围
- `orchestrator.py`：hub 任务的确认机制、反馈注入、步骤回退逻辑
- `agent_runner.py`：Prompt 上下文注入、反馈融合
- `state_manager.py`：hub 任务的步骤级回退、知识隔离存储
- `.serena/memories/hub/`：新增的知识隔离目录结构
- `lib/web_console.py` / `user_interface.py`：确认页面的修改意见交互
- `stream_renderer.py`：摘要表格显示步骤级费用明细

#### 向后兼容性
- 现有 dev 任务工作流完全保持不变
- hub 任务是新增模块功能，不影响现有 dev 开发流程
- 修改意见反馈是可选操作，不提供反馈时任务正常继续
- 知识隔离只对 hub 任务适用，dev 任务知识库保持原样

#### 语义说明
- **Hub 任务**：特定领域（如合同审查）的专业工作流，区别于通用开发任务
- **Manifest**：hub 模块的工作流定义文件，类似 dev 任务的工作流模板
- **知识隔离**：将不同领域的知识分开存储，提升知识的相关性和精准度
- **确认反馈注入**：用户的修改意见自动融入后续步骤的 AI Prompt

---

### 2026-03-17（第十次更新 - 语义路由 API 增强与命令扩展）

#### 新增：opus agents 命令 JSON 输出参数
- **变更说明**：`opus agents` 命令新增 `--json` 参数，支持 JSON 格式输出
- **命令格式**：`opus agents --json`
- **输出内容**：所有可用 Agent 模块的详细信息（JSON 格式）
- **应用场景**：第三方系统集成、脚本自动化、模块列表查询
- **示例输出**：结构化的模块清单，便于程序化处理

#### 改进：自动模块信息同步
- **变更说明**：每次执行 opus 命令时，系统自动同步最新的模块信息到 CLAUDE.md
- **同步开销**：约 10ms，性能影响可忽略
- **同步内容**：Agent 模块列表、功能描述、工作流说明
- **用户体验**：无需手动维护 CLAUDE.md 模块清单，始终保持最新状态

#### 改进：语义路由 API 请求格式升级
- **变更说明**：语义路由请求格式从纯 prompt+JSON 文本改为官方 Function Calling
- **技术意义**：采用标准的 Claude API Function Calling 接口，提升请求的稳定性和可靠性
- **用户影响**：无感知变更，仅后端实现优化，使用方式不变

#### 改进：语义路由 API 认证优化
- **变更说明**：路由 API 认证来源调整，优先读取 settings.json（Claude Code 用户配置）
- **优先级**：`settings.json` > `config.json` > 默认值
- **安全性提升**：从用户私人配置读取敏感信息，而非项目配置文件
- **应用场景**：多项目场景下，每个项目可独立配置路由 API 认证

#### 改进：路由 API 超时延长
- **变更说明**：route() 外层超时从 10 秒延长到 20 秒
- **原因**：考虑网络延迟和 API 服务器处理时间
- **影响**：任务启动时语义路由阶段最多等待 20 秒
- **用户体验**：在网络不稳定环境下更容易成功完成路由判定

#### 改进：语义路由 API 错误处理增强
- **变更说明**：API 失败行为从\"静默回退到开发流\"改为\"输出错误提示 + 模块列表 + 停止执行\"
- **新行为**：
  1. 若路由 API 不可用，系统输出具体错误提示
  2. 列出所有可用的 Agent 模块清单
  3. 停止执行，等待用户使用 `@模块ID` 重新指定模块
- **用户体验**：显式提示失败原因，而非隐默回退，便于诊断和处理
- **恢复方式**：用户可通过命令行 `@` 语法指定模块重试，例如 `opus \"任务描述\" @contract_service`

#### 影响范围
- `opus.py`：命令行参数解析新增 `agents --json` 选项
- `orchestrator.py`：语义路由 API 调用、认证读取、超时配置、错误处理
- `agent_runner.py`：settings.json 认证读取逻辑
- `CLAUDE.md`：模块信息自动同步更新
- 故障排除文档：新增语义路由 API 失败处理指南

#### 向后兼容性
- 现有 opus 命令行格式保持不变（新增 `agents --json` 为可选参数）
- 语义路由失败时改为显式停止，相比原有的静默回退行为，用户可更好地理解和处理
- 路由 API 认证从 config.json 优先改为 settings.json，但两者同时保持支持，确保兼容

---

### 2026-03-06（第九次更新 - 移动端控制台新增）

#### 新增：移动端控制台（Mobile Web Console）
- **变更说明**：针对移动设备（手机、平板）优化的任务控制台，提供与桌面版本同等的功能体验
- **新增路由**：
  - `/v6/m`：移动端控制台主页（任务列表、实时进度、费用统计）
  - `/v6/m/login`：移动端登录页面
- **路由注册**：confirm_server 启动时额外注册 3 条 Mobile 路由，现有路由行为保持不变（向后兼容）
- **初始化处理**：first_run 中间件扩展，未初始化时 `/v6/m` 也会自动重定向到 `/v6/console/setup` 进行初始化
- **认证体系**：共享桌面版 Web Console 的认证机制，无需单独设置密码
- **功能支持**：
  - 浏览任务列表和详情
  - 实时查看工作流进度表
  - 暂停/恢复/终止任务
  - 查看费用统计和 Token 消耗
  - 预览输出文档和成本详情
- **响应式设计**：自适应各尺寸移动设备屏幕，优化移动端用户体验
- **用户体验**：简化的移动端界面，无需滚动查看完整工作流，特别适合随时随地监控任务进度

#### 影响范围
- `lib/confirm_server.py`：新增 3 条 Mobile 路由注册
- `lib/web_console.py`：移动端 UI 组件开发
- `middleware/first_run.py`：初始化检测扩展（支持 `/v6/m` 重定向）

#### 向后兼容性
- 现有桌面版路由（`/v6/console`、`/v6/tasks` 等）行为完全保持不变
- 移动端为新增路由，不影响现有功能
- 共享同一套认证体系，无需额外配置

---

### 2026-03-05（第八次更新 - 工作流显式指定 + 技术栈自动检测）

#### 新增：CLI 工作流显式指定参数
- **变更说明**：新增 `--workflow` 参数，用户可通过命令行显式指定工作流类型
- **参数格式**：`opus --workflow <type> "任务描述"`
- **支持的类型**：`feature`、`bug_fix`、`refactor`、`hotfix`、`optimization`、`auto`（自动分类）
- **功能优势**：指定工作流后跳过 AI 自动分类阶段（architect 架构设计阶段仍执行），节省约 $0.01~0.02 成本
- **示例命令**：
  ```bash
  opus --workflow feature "添加用户认证功能"
  opus --workflow bug_fix "修复登录页面崩溃"
  ```

#### 新增：Web Console 工作流快捷按钮区
- **位置**：Web Console 底部，输入框下方第二行
- **快捷按钮**：6 个工作流类型快捷按钮（新功能、缺陷修复、代码重构、紧急修复、性能优化、自动分类）
- **操作方式**：点击按钮后自动填入工作流前缀到输入框，用户只需补充任务描述
- **用户体验**：无需记忆命令行参数，直观快速启动指定工作流任务

#### 新增：项目技术栈自动检测
- **触发时机**：首次在新项目中运行 opus 任务
- **检测方式**：自动扫描项目文件（package.json、go.mod、requirements.txt、build.gradle 等）
- **生成物**：生成 `tech-stack.md` 记忆文件，记录项目技术栈和主要依赖
- **存储位置**：项目 Serena 记忆库（`.serena/memories/tech-stack.md`）

#### 新增：技术栈上下文自动注入
- **受影响角色**：架构师、后端开发、前端开发、嵌入式开发、联调工程师、QA 测试、修复工程师
- **上下文内容**：这 7 个角色的 Prompt 中自动包含项目技术栈信息
- **功能优势**：
  - 提升 Agent 的技术匹配度，生成更符合项目技术选择的代码
  - 减少不必要的技术选择讨论和诊断步骤
  - 改进代码质量和开发效率

#### 改进：知识工程师权限扩展
- **新增权限**：知识工程师现在可以读取和维护 `tech-stack.md` 记忆
- **使用场景**：当项目依赖升级、新增技术栈或框架后，知识工程师可更新对应记忆
- **后续效果**：所有后续任务自动加载最新的技术栈信息，保持 Agent 上下文的时效性

#### 影响范围
- `opus.py`：CLI 参数解析新增 `--workflow` 选项
- `orchestrator.py`：工作流路由逻辑，支持显式指定跳过自动分类
- `lib/web_console.py`：Web Console 前端新增工作流快捷按钮区（底部第二行）
- `agent_runner.py`：技术栈上下文注入到 7 个技术角色的 Prompt
- `role_templates/`：架构师、开发、测试等角色模板更新，支持技术栈上下文占位符
- `state_manager.py`：首次运行检测和 tech-stack.md 生成逻辑
- 知识工程师角色权限：role_templates/knowledge_engineer.md

---

### 2026-03-02（第七次更新 - 直播面板与任务展示优化）
#### 新增：状态展示统一映射
- **变更说明**：任务切换弹窗与直播面板的状态展示完全一致，使用统一的状态映射表
- **状态映射**：确保所有界面展示的任务状态完全一致，避免信息不一致

#### 新增：任务列表智能排序
- **变更说明**：任务列表默认排序策略优化
- **排序规则**：进行中任务（running/paused）优先显示 + 按创建时间倒序
- **用户体验**：活跃任务始终位于列表顶部，便于快速查找和操作

#### 新增：完整工作流步骤渲染
- **变更说明**：工作流步骤列表完整渲染，包含待执行步骤
- **实现方式**：根据 task_type 加载对应的工作流模板
- **用户体验**：用户可预览完整的工作流流程，了解后续待执行的步骤

#### 新增：任务类型标签
- **变更说明**：直播面板顶部新增任务类型标签
- **标签格式**：`任务类型：缺陷修复（bug_fix）`
- **支持类型**：支持 5 种任务类型（bug_fix、feature、refactor、hotfix、optimization）
- **功能增强**：点击类型标签可展示完整的工作流流程图

#### 优化：任务名称优先级
- **变更说明**：任务名称展示优先级调整
- **优先顺序**：优先使用 AI 提炼的 task_name 字段，降级使用 description
- **优势**：AI 提炼的标题更精准、简洁，提升任务可读性

---

### 2026-03-01（第六次更新 - 公网域名配置优化）

#### 新增：网络 Tab 配置功能
- **变更说明**：Web Console 设置页新增「网络」Tab，用于配置公网域名
- **功能用途**：设置后用于生成文档预览链接、确认页面链接
- **配置位置**：Web Console → 设置 → 网络 Tab

#### 优化：链接生成统一
- **变更说明**：文档预览链接、确认页面链接统一使用 `get_base_url()` 函数生成
- **一致性**：确保所有外链使用统一的域名配置

#### 配置变更：custom_domain 默认值
- **变更说明**：`config.json` 中 `custom_domain` 默认值从 `opus.bingbing.asia` 改为空字符串
- **影响**：新部署可按需手动配置公网域名，用于生成外部可访问链接

#### 移除：tunnel_url 逻辑
- **变更说明**：移除 tunnel_url 相关逻辑
- **原因**：统一使用公网域名配置生成外部链接

---

### 2026-02-26（第五次更新 - 密钥安全隔离与部署增强）

#### 新增：敏感字段安全隔离
- **变更说明**：config.json 中所有敏感字段（Redis 密码、Web Console 密码等）从真实值改为 `YOUR_*` 占位符
- **安全性提升**：防止误提交含真实凭证的配置文件到版本控制
- **向后兼容**：现有部署无需修改，真实值通过环境变量覆盖

#### 新增：环境变量覆盖机制
- **变更说明**：支持 10+ 个环境变量覆盖 config.json 对应字段
- **支持的变量**：`REDIS_HOST`、`REDIS_PORT`、`REDIS_PASSWORD`、`CLAUDE_API_KEY`、`WEB_CONSOLE_PASSWORD`、`DEFAULT_MODEL`
- **优先级**：环境变量 > config.json > 默认值
- **应用场景**：Docker 部署、CI/CD 流水线、多环境配置切换

#### 新增：Docker Compose 一键部署
- **变更说明**：支持 `docker-compose up -d` 完整堆栈部署
- **包含组件**：Opus V6 应用、Redis 缓存、Web Console
- **优势**：快速启动、环境隔离、易于扩展
- **操作方式**：详见操作指南中的「部署方式」章节

#### 新增：Web Console 认证升级
- **变更说明**：Web Console 认证从明文 token 升级为 bcrypt 哈希密码验证
- **安全性**：密码采用 bcrypt 加密存储，不支持明文查询
- **修改密码**：在 Web Console 设置页支持密码修改功能

#### 新增：Setup Wizard 首次引导
- **变更说明**：全新部署首次访问 Web Console 必然显示 Setup Wizard 引导页
- **引导内容**：初始化系统配置、设置访问密码
- **路由保护**：未完成引导时，所有受保护路由自动重定向到引导页
- **用户体验**：确保首次部署安全顺利完成配置

#### 新增：Web Console 会话管理
- **变更说明**：修改密码后，旧 Cookie 在 5 秒宽容期后失效
- **安全考虑**：防止旧会话被滥用，同时给用户 5 秒刷新缓存的时间
- **用户体验**：修改密码后无需立即重新登陆，5 秒后自动要求重新认证

#### 新增：设置页模型配置
- **变更说明**：Web Console 设置页新增模型配置功能，支持修改 LLM 模型偏好
- **实时生效**：模型配置修改后立即写入 config.json 并热加载
- **无需重启**：修改立即生效，无需重启应用

#### 影响范围
- `config.json`、`lib/config_v3.json`
- `lib/confirm_server.py`（Web Console）
- `lib/web_console.py`（Setup Wizard、设置页）
- `agent_runner.py`、`orchestrator.py`（环境变量读取）
- Dockerfile、docker-compose.yml（新增）

---

### 2026-02-20（第四次更新）

#### 改进：子任务中断恢复机制
- **变更说明**：子任务 suspend 后 resume 时，系统从上次完成的步骤继续执行，不再重新运行已完成步骤
- **应用场景**：大需求多任务流程中，若某个子任务被中断，恢复时自动跳过已完成部分
- **操作命令**：`opus --resume` 自动检测并恢复最后一个未完成任务

#### 改进：QA 测试范围判定逻辑
- **变更说明**：子任务的 QA 测试从全量判定改为范围内判定
- **通过条件**：测试通过率 >= 90% **且** 修改范围内无失败
- **范围外处理**：修改范围外的失败作为回归警告单独汇报，不阻止任务继续
- **应用场景**：有助于快速定位修复范围，减少无关回归警告对修复进度的影响

#### 改进：Bug-fix 循环派发精准度
- **变更说明**：修复工程师在 bug-fix 循环中只收到修改范围内的测试失败
- **优化效果**：修复工程师可精准定位需要修复的问题，范围外失败单独跟踪
- **三轮上限**：bug-fix 循环最多执行 3 轮，超过则自动回滚

#### 改进：确认请求秒级转发
- **变更说明**：确认请求创建后会在秒级内通过 stderr 通知转发到主代理
- **用户体验**：无需等待 hook 主动检测，确认请求更迅速地送达用户
- **技术实现**：优化跨进程通信，加速确认通知传递链路

---

### 2026-02-20（第三次更新）

#### 新增：Web 任务控制台
- **功能说明**：浏览器操控任务执行流，支持暂停、恢复、回退、终止等操作
- **访问地址**：`https://opus.bingbing.asia/v6/tasks`（任务列表）及 `/v6/tasks/{task_id}`（任务详情）
- **操作支持**：
  - 暂停运行中的任务
  - 从暂停状态恢复
  - 回退到指定步骤
  - 终止任务（二次确认）
- **确认链接改进**：Web 确认页面链接过期后，自动跳转到任务控制台，而非显示空白过期页面

#### 改进：预览文件持久化
- **存储位置**：预览文件从仅存 Redis 改为同时写入磁盘
- **可用性**：重启预览服务后，文档仍可通过原链接访问（此前重启后丢失）
- **影响范围**：preview_server.py 备份和恢复逻辑

---

### 2026-02-19（第二次更新）

#### 新增：直播页面完整任务总结表格
- **变更说明**：直播页面（`opus --stream`）和回放模式（`opus --stream --replay`）任务执行完毕时自动显示完整的总结表格
- **表格内容**：包含每个 Agent 的角色、使用模型、执行状态、耗时、费用、输出文档、预览链接等信息
- **自动生成**：无需用户手动操作，任务完成后系统自动生成并展示
- **回放支持**：历史任务可通过 `--replay` 模式重新查看完整的执行过程和总结表格
- **数据来源**：从 cost.json 和任务状态文件读取
- **影响范围**：stream_renderer.py、orchestrator.py、state_manager.py

#### 改进：cost.json 字段扩展
- **变更说明**：cost.json 新增三个可选字段，扩展成本记录的完整性（向后兼容）
- **新增字段**：
  - `duration`：Agent 执行耗时，格式 `Xm YYs`
  - `output_file`：生成的输出文件名（如 `02-design.md`）
  - `preview_url`：文件在线预览链接（如 `https://opus.bingbing.asia/preview/xxx`）
- **向后兼容性**：现有 cost.json 文件无需修改，新字段完全可选
- **应用场景**：
  - 直播页面总结表从 cost.json 读取显示文件和费用信息
  - 任务历史统计聚合计算时支持文件链接
  - 与外部系统集成时提供完整的执行元数据
- **实现类别**：state_manager.py、stream_renderer.py、user_interface.py

### 2026-02-19

#### 新增：任务执行全链路上下文信息展示
- **变更说明**：Web 确认页面、终端确认提示和跨进程通知新增丰富的任务上下文信息，帮助用户快速了解当前任务状态
- **Web 确认页面**：新增 context-card 信息卡片区域，展示角色、费用、进度等关键信息
- **终端确认提示**：新增完整的上下文展示（任务名、角色、步骤、费用、进度等）
- **跨进程通知**：stop_guard hook 通知新增任务名、角色、步骤、费用信息
- **stderr 标记**：扩展为 6 段格式，末尾追加 role_display 和 step_display
- **影响范围**：user_interface.py、orchestrator.py、hooks/stop_guard.py、lib/confirm_server.py

#### 优化：任务历史表格列展示和描述列宽
- **变更说明**：任务历史表格从 4 列扩展到 5 列，新增「耗时」列
- **新增列位置**：在"描述"列与"状态"列之间
- **列内容**：显示任务执行耗时（如 `3m 45s`），运行中任务显示 `⏳ 运行中`
- **描述列优化**：描述列宽从 40 字符缩减至 36 字符，优化表格宽度
- **实现类别**：orchestrator.py 中的任务历史查询逻辑
- **影响范围**：命令行 `opus --history` 输出表格

#### 新增：工作流进度表"模型"列
- **变更说明**：工作流进度表从 8 列增加到 9 列
- **新增列位置**：在"角色"列之后
- **列内容**：显示当前 Agent 使用的 LLM 模型（如 opus、sonnet、haiku）
- **降级显示**：当模型自动降级时显示完整链路（如 `opus→haiku`）
- **实现类别**：user_interface.py 中的 `AgentProgressDisplay._render_table()` 方法
- **影响范围**：终端进度可视化

#### 改进：模型降级链优化
- 首选模型不可用时自动降级至备用模型
- 完整链路在模型列实时展示
- 支持按角色自定义模型策略

---

**最后更新**：2026-03-05
**版本**：1.3
**维护人**：Opus V6 系统
