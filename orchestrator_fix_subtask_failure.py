# 修复方案：子任务失败时更新父任务状态

## 问题描述
当子任务（如 sub-1 和 sub-8）失败时，父任务状态不会自动更新为 "failed"，导致：
1. 父任务状态仍显示 "running"
2. 移动端和PC端状态不一致
3. 任务无法被正确暂停或终止

## 修改位置
orchestrator.py - _execute_sub_task 方法

## 修改内容
在子任务失败时，需要：
1. 检查失败的子任务是否是关键任务
2. 如果是关键任务失败，提前终止父任务
3. 更新父任务状态为 "failed"

## 具体修改